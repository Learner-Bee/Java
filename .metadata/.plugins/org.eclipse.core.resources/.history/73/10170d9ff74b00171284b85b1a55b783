package com.lily.directory;

import java.io.*;

public class DirOfDir {

	public static void main(String[] args) {
		//获取指定目录下的所有目录
		//1.指定目录
		//2.判断是否是目录
		// 2.1如果是，获取目录下面的文件，循环遍历文件名，判断是否为目录
		//   2.1.1如果不是目录，continue继续执行循环
		//   2.1.2如果是目录，输出目录名
		// 2.2如果不是，输出提示信息。不是目录
		
		File f=new File("../Learn/Java/");
		getDir(f);
		
		
	}
	public static void getDir(File f){
		
		
	}

}
